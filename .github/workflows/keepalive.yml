name: Keepalive

on:
  schedule:
    - cron: '0 0 * * 0'   # 每周日 00:00 UTC 自动执行一次
  workflow_dispatch:       # 允许你手动触发

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep repo active
        run: echo "Keepalive run on $(date)"
